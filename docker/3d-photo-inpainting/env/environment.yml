name: base
channels:
  - defaults
  - pytorch
dependencies:
  - matplotlib=3.1.3
  - pip:
    - opencv-python==4.2.0.32
    - piexif==1.1.3
    - azure-storage-queue
    - azure-storage-blob
    - applicationinsights
