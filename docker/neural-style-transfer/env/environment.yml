name: base
channels:
  - defaults
  - pytorch
dependencies:
  - matplotlib
  - pip:
    - opencv-python
    - piexif==1.1.3
    - azure-storage-queue
    - azure-storage-blob
    - applicationinsights
